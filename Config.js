.pragma library

// Convex deployment that holds the leaderboard. Override with the
// TYPARCHY_CONVEX_URL environment variable (e.g. to point at a local backend).
var convexUrl = "https://coordinated-crow-518.convex.cloud"
